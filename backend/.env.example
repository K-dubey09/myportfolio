# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Firebase Configuration
# IMPORTANT: Store your firebase-service-account.json file locally in the backend folder
# NEVER commit this file to git! It's already in .gitignore
# The app will automatically load it from backend/firebase-service-account.json

# OR use Base64 encoded service account (for deployment)
# To generate: base64 -w 0 firebase-service-account.json (Linux/Mac)
# Or: [Convert]::ToBase64String([IO.File]::ReadAllBytes("firebase-service-account.json")) (Windows PowerShell)
# FIREBASE_SERVICE_ACCOUNT_BASE64=

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Session Secret
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Email Configuration (if implementing contact form)
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
# EMAIL_TO=your-email@gmail.com